/** 기본 구면 배치 반경 (depth 1) */
export const DEFAULT_BASE_RADIUS = 0.9;

/** 깊이별 반경 증가량 (depth 2+) */
export const DEFAULT_RADIUS_STEP = 0.9;

/** 링 반경 전환 기준 자식 수 */
export const RING_LARGE_THRESHOLD = 20;

/** 자식이 많을 때 링 반경 */
export const RING_RADIUS_LARGE = 0.8;

/** 자식이 적을 때 링 반경 */
export const RING_RADIUS_SMALL = 0.4;

/** 방향 벡터와 up 벡터 정렬 판별 임계값 */
export const UP_ALIGNMENT_THRESHOLD = 0.9;
